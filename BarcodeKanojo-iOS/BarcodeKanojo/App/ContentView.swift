import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Barcode Kanojo")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
